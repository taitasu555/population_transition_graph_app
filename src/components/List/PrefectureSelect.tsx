import React, { FC } from 'react'
import styles from '../../styles/Home.module.css'

export const prefectureSelect: FC = () => {
  return <footer className={styles.footer}>Author by Taishin</footer>
}
